KLU-ERP React (final)
Run:
  npm install
  npm run dev
Notes:
  - Login page is static (no auth) — submit redirects to dashboard
  - data.js and charts.js are reused from the uploaded files (placed in public/js)
  - Chart.js is loaded via CDN in index.html
"# ERP--FEDF-Project-" 
